/**
 * RED tests for add-goal-focus-locking — Unit F: /goal-focus override flow.
 * RED stub — full harness tests in GREEN phase.
 */
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { acquireLock } from "../extensions/goal-lock.ts";

describe("/goal-focus override flow (TODO: harness tests — RED stub)", () => {
	it("RED stub: imports from goal-lock.ts (fails until GREEN)", () => {
		assert.fail("RED: goal-lock.ts not yet implemented");
	});
});
