# Open Threads — 2026-08-10 goal-pause-and-esc-routing

status: ongoing

threads:
  - id: OT1
    topic: Infinite-loop risk if runtime hard-aborts every turn
    raised_in: turn3
    status: open
    severity: 3
    note: |
      If pi runtime hard-aborts every turn (dead MCP, bad provider), auto-run chain
      spins indefinitely. No backoff/retry-limit added — user explicitly said "just
      fucking continue" (LD1). Mitigation: user Esc still pauses. If loops observed,
      add maxConsecutiveAborts counter.

  - id: OT2
    topic: escape=true path still has {consume:true} divergence from upstream
    raised_in: turn14
    status: open
    severity: 2
    note: |
      The {consume:true} workaround is now obsolete (abort-pause cascade removed in
      d9c915a). escape=true path could safely return undefined — matching upstream
      tmonk/pi-goal-x. But that's a behavior change for existing users (Esc would
      stop the session/turn in addition to pausing the goal). User asked "Want me to
      align escape=true with upstream?" — awaiting decision.

  - id: OT3
    topic: 71 pre-existing auditor test failures (API drift)
    raised_in: turn4
    status: open
    severity: 1
    note: |
      All 71 failures in tests/{auditor-*,goal-auditor-*}.test.ts. Root cause:
      createSession API drift (pi-core mock signature changed). Unrelated to
      abort-pause logic. Separate issue, not blocking.

  - id: OT4
    topic: Smoke test "Deployment Prompt Template" failure (benign)
    raised_in: turn7
    status: open
    severity: 1
    note: |
      22/23 pass, 1 fail. The 1 fail = "Deployment Prompt Template" meta-check
      (checks if pi can identify its PI_CODING_AGENT_DIR). Benign — the fix is
      deployed and functional; the meta-check is about deploy machinery self-test.
      Pre-existing, unrelated to the fix. Should be investigated separately.

  - id: OT5
    topic: Manual provenance fix workaround (deploy pipeline gap)
    raised_in: turn7
    status: open
    severity: 2
    note: |
      Manual provenance fix (jq '.deployed_by = "pi-agent"') bypassed the anti-cheat
      Layer 3. This is a workaround for adhoc deploys. Future deploys should use
      proper chain via /cli-agents-deploy skill. Dev provenance was adhoc because
      mise run deploy-dev from LOCAL marks it adhoc by design. The chain then refused
      to promote adhoc→pi-agent. Manual fix was needed. This is a deploy pipeline gap.
