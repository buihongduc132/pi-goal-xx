node_modules/
coverage/
*.log
.DS_Store
# Runtime goal state (traces, ledger, locks, goal records) — never commit.
.pi/goals/
# Runtime process traces at .pi root — never commit.
.pi/print-clean-exit-trace.jsonl
.pi/*-trace.jsonl
# Git worktrees (local checkouts) — never commit.
.worktrees/
.wt-*/
